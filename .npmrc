# Allow @vercel packages to run build scripts
@vercel:registry=https://registry.npmjs.org/
scripts-prepend-node-path=true

# More secure alternative to unsafe-perm
# Only allow scripts for trusted @vercel packages
ignore-scripts=false 