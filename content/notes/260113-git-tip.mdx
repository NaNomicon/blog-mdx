export const metadata = {
  title: "Git stash apply vs pop",
  publishDate: "2026-01-13",
  collection: "Development",
  description: "A quick git tip.",
  category: "Tools",
  tags: ["git"]
};

- `git stash pop`: Applies the stash and removes it from the list.
- `git stash apply`: Applies the stash but keeps it in the list.

I prefer `apply` just in case something goes wrong during the merge!
